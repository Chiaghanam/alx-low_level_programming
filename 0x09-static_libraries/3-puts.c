#include "main.h"
/*_puts: import*/
void _puts(char *str);
#include<stdio.h>
/**
 * main - Entry point
 * _Puts - import function
 * _puts - import function
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}

